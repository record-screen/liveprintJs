name: Build and Deploy

on:
  push:
    branches:
      - test  # Change this to your main branch name

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'  # Change this to your Node.js version

      - name: Install Gulp
        run: npm install gulp-cli -g

      - name: Install dependencies
        run: npm install

      - name: Build for local environment
        run: gulp buildStaging

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@3.7.1
        with:
          token: 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
          branch: test  # Change this to the branch you want to deploy to
          folder: dist      # Change this to the directory containing your built files
          clean: true       # Optional: Set to true to clean the target branch before deployment

      # Add more steps as needed for staging and production builds
